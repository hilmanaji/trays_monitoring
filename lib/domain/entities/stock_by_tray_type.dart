class StockByTrayType {
  const StockByTrayType({
    required this.trayTypeId,
    required this.trayTypeName,
    required this.total,
  });

  final int trayTypeId;
  final String trayTypeName;
  final int total;
}
